{
    "_description": "FSDP2 + Expert Parallel configuration for DeepSeek-V4 MoE training.",
    "_requires": "torch>=2.4.0",
    "_note": "Use together with `--expert_parallel true`. The MoE experts are physically sharded across GPUs by expert_parallel and are excluded from FSDP sharding; all non-expert params are sharded by FSDP2.",

    "fsdp": "full_shard auto_wrap",
    "fsdp_config": {
        "fsdp_version": 2,
        "reshard_after_forward": true,
        "auto_wrap_policy": "TRANSFORMER_BASED_WRAP",
        "cpu_ram_efficient_loading": true,
        "state_dict_type": "SHARDED_STATE_DICT",
        "activation_checkpointing": false
    }
}
